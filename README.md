# Initial setting

```
npm install -D babel-plugin-module-resolver prettier redux-devtools

npm install axios formik js-cookie moment redux redux-thunk redux-saga styled-components immer daggy  connected-react-router
```
